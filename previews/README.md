# Previews

One screenshot per theme, named `<theme-id>.png`, referenced from `skins.json`
as `previewUrl` and shown on the theme card in Skin Manager.

Capture from the Home screen at 1920×1080 with a populated library, so rows of
poster art are visible. Keep files under about 500 KB.
